// Time and Space Complexity : parameters on which an interviewer judges you/your code. 
// Time complexity depends on system / depends on configuration, it doesn't means time taken.
// Rate at which time taken incr wrt input size.
//(time vs input) : theta is angle between.

// worst case complexity or upper bound complexity.
/* Big-O Notation: -> O(n*3)
// here it checks , print , increment , therefore for every-time(3) :-> (nx3) that'swhy :O(n*3)

for(i=1;i<=n;i++) {
    cout << "adi" ;
}
*/

// the code will run for O(n*3) times.

// time complexity to computed always in terms of worst case scenario, avoiding constants(when i/p size is very large), avoid lower values.
